import {} from 'three';

function makeRenderer () {
    return W.promise( function ( resolve, reject ) {
        resolve( {} );
    });

}

function initRenderer ( renderer ) {
    return W.promise( function ( resolve, reject ) {
        resolve( renderer );
    });
}

var Renderer = {

}

export {
    
};

